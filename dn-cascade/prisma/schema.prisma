
/// Generates the Prisma Client for TypeScript/Node.
generator client {
  provider = "prisma-client-js"
}

/// Primary datasource. Uses SQLite for local development.
/// The connection string is provided via the DATABASE_URL env var.
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
